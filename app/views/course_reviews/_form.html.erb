<%= simple_form_for([@course, @review]) do |f| %>
<div class="form-inputs">
  <%= f.input :title  %>
  <%= f.input :content  %>
  <%= f.input :rating, collection: [1,2,3,4,5]  %>
  <div class="search-4ca">
  <%= f.button :submit, "Submit", class: "button-green-primary" %>
  </div>
</div>
<% end %>
